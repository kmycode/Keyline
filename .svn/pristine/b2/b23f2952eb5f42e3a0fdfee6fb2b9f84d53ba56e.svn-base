/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package keyline.view;

import java.net.URL;
import java.util.ResourceBundle;
import javafx.beans.property.DoubleProperty;
import javafx.beans.property.SimpleDoubleProperty;
import javafx.fxml.FXML;
import javafx.fxml.Initializable;
import javafx.scene.layout.AnchorPane;

/**
 * FXML Controller class
 *
 * @author KMY
 */
public class MainWindowCharacterModeController implements Initializable {

	@FXML
	AnchorPane rootPane;

	@FXML
	AnchorPane mainScreenPane;

	private DoubleProperty width = new SimpleDoubleProperty();

	/**
	 * Initializes the controller class.
	 */
	@Override
	public void initialize (URL url, ResourceBundle rb) {
		// TODO
	}

	public void setWidthProperty (DoubleProperty wp) {
		this.width.bind(wp);
		this.width.addListener((x) -> this.redraw(((DoubleProperty) x).get()));
	}

	public void redraw (double width) {
	}

}
